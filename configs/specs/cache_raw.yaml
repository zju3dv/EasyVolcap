dataloader_cfg:
    dataset_cfg:
        disk_dataset: False
        cache_raw: True
        n_rays: 128
    batch_sampler_cfg:
        batch_size: 64 # will sample from all images (this should be only used for static scenes)
    num_workers: 64
